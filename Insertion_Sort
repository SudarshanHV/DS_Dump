int arr[]={4,6,3,5,2,1};
		for(int i=1;i<6;i++){
			int key=arr[i];
			int j=i-1;
			for(j=i-1;j>=0;j--){
				if(arr[j]>key){
					int temp=arr[j+1];
					arr[j+1]=arr[j];
					arr[j]=temp;
				}
			}
		}
